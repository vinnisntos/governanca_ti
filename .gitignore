# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# documentação interna (arquitetura, ADR, setup, progresso) — não sobe pro git
/ARQUITETURA_TECNICA.md
/ARCH_DECISIONS.txt
/SETUP.md
/README.txt

# planilha financeira sincronizada do Google Drive (dados de fornecedor/custo) — não sobe pro git
/Assinaturas*.xlsx

# chave de Service Account do Google Cloud baixada em JSON — nunca sobe pro
# git; o conteúdo dela vira a env var GOOGLE_SERVICE_ACCOUNT_KEY (base64) em
# .env.local, e o arquivo é apagado depois de convertido.
/governanca-ti-integracoes-*.json
